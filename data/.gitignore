# Ficheros de runtime en la raíz
settings.json
users.json
.admin_pass

# Directorios gestionados por data-init (nunca versionar)
skills/public/
skills/private/

# Directorios de datos en tiempo de ejecución — nunca versionar
agents/
connections/
memory/
skills/
accounts/

# Base de datos SQLite (runtime, nunca versionar)
hub.db
hub.db-shm
hub.db-wal
